package chapterThree;

public class ClockDriver {
    public static void main(String[] args) {

        Clock clock = new Clock(23, 98, 43);
//        clock.displayTime(clock.getHour(),clock.getMinute(), clock.getSecond());
       clock.displayTime();

    }
}

